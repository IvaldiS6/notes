pass a sprite between 2 canvases
animate a static page only while arrays/lists are populated 

push projects to GitHub
1. > git config --global user.email <email address>
2. > git config --global user.name <your name or pseudonym>
3. > git init
4. > git remote add origin <URL to repository>
5. > git branch -M main
6. > git push origin HEAD

navigating WSL
/mnt/c/ is the same as C:/

delete folders containg things
rm -r <folder> (-v verbose)

Update information list location
/Etc/apt/sources.list.d

cloud start here:
cargo install sugar-cli --locked

solana
solana config get 
solana config set --url https://api.<clster>.solana.com

CRON
add path to crontab
install postfix
  
HOLDER SCRAPE
Step 1: Magic Eden's Hash List Finder for NFTs & SPL tokens:
https://magiceden.io/mintlist-tool

Step 2: Take snapshot of holders from Hash List: 
https://pentacle.tools/holder-snapshot

Step 3: Convert JSON to CSV
https://www.convertcsv.com/json-to-csv.htm


Turn CSV into .txt file with all addresses copy & pasted to match the amount of nfts they hold
